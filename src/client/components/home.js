import React from 'react';

const Home = (props) => {
    return <div>
        This is Home Page.
        <button onClick={() => {console.log("hello")}}>Click</button>
    </div>
} 

export default Home;